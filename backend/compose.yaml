services:
  mysql:
    image: mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: bankdb
      MYSQL_USER: bankdb
      MYSQL_PASSWORD: bankdb
    ports:
      -"3304:3306"
    volumes:
      -my-db:/var/lib/mysql
volumes:
  my-db:
